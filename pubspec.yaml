name: trailwatt
description: Trailwatt - translates training targets into real routes.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:     # pt/en, following the device locale
    sdk: flutter
  google_fonts: ^6.2.1       # Fraunces / Inter / IBM Plex Mono
  flutter_map: ^7.0.2        # for screens 04/05/02a2/02b3 (route + zone maps)
  flutter_map_dragmarker: ^7.0.0  # draggable waypoints on screen 05
  latlong2: ^0.9.1
  http: ^1.2.2               # OSRM road-snapping / routing calls
  table_calendar: ^3.1.2     # for screens 08a/08b/08c (calendar week/month)
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  generate: true             # builds AppLocalizations from lib/l10n/*.arb
